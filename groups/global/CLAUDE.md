# Global Agent Instructions

Global instructions have moved to `instructions/global.md` and are now assembled
into each agent's CLAUDE.md at spawn time. This file is kept empty to avoid
duplication (the agent-runner still loads it for non-main containers).
